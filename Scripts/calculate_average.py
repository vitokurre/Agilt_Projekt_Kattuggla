def calculate_totalaverage(total_time, divide_byten):
    """"Calculate the average time and return number"""
    number1 = float(total_time)
    number2 = float(divide_byten)
    #sum = number1 / number2
    return float(number1 / number2)
